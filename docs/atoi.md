# atoi

## 函数签名

```C
CCINT32 cc_atoi(const char *str_num);
```

参数列表：
- `str_num`：数字字符串。

返回值：转换后的32位整数。

## 错误处理

**空指针、空字符串：**
返回0。

**非数字字符：**
命令行日志输出对应字符，并返回0。

**数字过大溢出：**
正数溢出返回值限缩到 `INT32_MAX`
负数溢出返回值限缩到 `INT32_MIN`